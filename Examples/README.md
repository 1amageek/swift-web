# SwiftWeb Examples

| Example | Purpose |
|---|---|
| `CounterApp` | Canonical sample for `@Page`, SwiftWebUI layout, client `@State`, WASM hydration, and distributed server actions. |

Run the counter sample:

```bash
swift-web dev --package-path Examples/CounterApp
```

Open `http://127.0.0.1:3000/counter`.
