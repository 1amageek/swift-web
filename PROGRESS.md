# Progress

- [x] REL-AS Release `swift-actor-system` 0.1.0 as the single long-term owner of the transport-neutral Actor runtime while preserving the existing public/runtime semantics; completed in commit `f3f80a00e2e4c02f873e1468322c39b0f05bb75e`; API redesign, an Embedded HTTP host, cloud deployment, and unrelated hardening are non-goals `depends:none` `parallel:none`

- [ ] REL-HTML Release SwiftHTML 0.16.0 for the state-invalidation API required by SwiftWeb, without changing component rendering semantics `depends:none` `parallel:none`
  - [ ] REL-HTML.1 Confirm `0.15.0..0d2fb65` is the additive invalidation-handler delta, align affected README/Embedded commands to the pinned 2026-08-14 Swift 6.4 toolchain, run the focused SwiftHTML host regression that proves one callback per dirty cycle outside the store mutex, reuse the final SwiftWeb host concurrency test and real standard-WASM browser counter flow to prove the actual `ClientBundleRuntimeEntrypoint` consumer schedules a post-dispatch reconciliation, and require the final generated Embedded-WASM package to compile/link the same mirrored `StateStore` and runtime-entrypoint sources without presenting the separate `SwiftHTMLClientRuntime` example or compile/link alone as Embedded callback runtime evidence; obtain this designer’s conformance review, commit only release metadata/documentation if needed on `main`, then push, annotate/tag `0.16.0`, publish the public GitHub Release, and verify peeled tag, release target, `origin/main`, and a fresh version consumer all agree `depends:none` `parallel:none`

- [ ] REL-SSL Release swift-ssl 0.4.0 as the stable owner of the direct TLS-record destination, resumption, and TLS 1.3 compatibility changes consumed by swift-tls; broader cryptography review is a non-goal `depends:none` `parallel:none`
  - [ ] REL-SSL.1 Confirm `origin/main` is an ancestor of the already-published `origin/codex/tls-stream-destination-output` branch and that the public API delta warrants 0.4.0, fast-forward local `main` without rewriting history, update only release documentation/metadata, run bounded `swift test` gates for the changed AEAD, TLS 1.3 record, handshake compatibility, resumption, and failure paths under the pinned toolchain, obtain this designer’s conformance review, commit and push, then annotate/tag `0.4.0`, publish the public GitHub Release, and verify peeled tag, release target, `origin/main`, and fresh version resolution agree; return to the designer instead of merging if ancestry or contracts differ `depends:none` `parallel:none`

- [ ] REL-TLS Release swift-tls 2.1.0 on the stable swift-ssl 0.4.0 contract while preserving the owned/scoped byte boundary and typed failure model `depends:REL-SSL` `parallel:none`
  - [ ] REL-TLS.1 Confirm `origin/main` is an ancestor of the already-published `origin/codex/tls-stream-destination-output` branch and that the public delta warrants 2.1.0, fast-forward local `main` without rewriting history, replace the swift-ssl revision with a version requirement beginning at 0.4.0, update only release documentation/metadata, run bounded canonical success/failure/resumption/close and direct-destination ownership tests on the pinned host and applicable WASM/Embedded profiles, obtain this designer’s conformance review, commit and push, then annotate/tag `2.1.0`, publish the public GitHub Release, and verify peeled tag, release target, `origin/main`, and fresh version resolution agree; return to the designer if ancestry or semantics differ `depends:REL-SSL` `parallel:none`

- [ ] REL-TLSNIO Release swift-tls-nio 0.1.0 as the NIO adapter over swift-tls 2.1.0, preserving ByteBuffer ownership, active-handshake write buffering, backpressure/order, failure, and shutdown contracts `depends:REL-TLS` `parallel:none`
  - [ ] REL-TLSNIO.1 Confirm the initial public API warrants 0.1.0, replace the swift-tls revision with a version requirement beginning at 2.1.0, run bounded TLSNIO embedded-channel and real NIO loopback tests covering handshake, active writes, decrypted inbound data, close/failure, and buffer lifetime under the pinned toolchain, update only release documentation/metadata, obtain this designer’s conformance review, commit on `main` and push, then annotate/tag `0.1.0`, publish the public GitHub Release, and verify peeled tag, release target, `origin/main`, and fresh version resolution agree `depends:REL-TLS` `parallel:none`

- [ ] REL-WEB Prepare SwiftWeb 0.12.0 to consume only released package versions and the standalone Actor source owner while preserving the existing Distributed Actor, `.actor(_:identity:)`, `ActorGroup`, `@RemoteActor`, HTTP/WSS, and generated-package behavior `depends:REL-AS,REL-HTML,REL-TLSNIO` `parallel:none`
  - [ ] REL-WEB.1 Add the SwiftWeb root package master `DESIGN.md` and the changed `Sources/SwiftWebDevelopment/PackageGeneration/DESIGN.md` and `Sources/SwiftWebRuntime/Actors/DESIGN.md` child contracts, recording that SwiftPM resolves `swift-actor-system` while generated standard/Embedded WASM packages mirror the required source targets from that resolved checkout; index unchanged module authorities without reproducing their text, and do not duplicate lower-level Actor contracts or redesign public APIs `depends:REL-AS,REL-HTML,REL-TLSNIO` `parallel:none`
  - [ ] REL-WEB.2 Replace root revision/path dependencies with released version requirements for SwiftHTML 0.16.0, swift-actor-system 0.1.0, swift-tls 2.1.0, and swift-tls-nio 0.1.0; make the existing runtime-source locator discover Actor targets from resolved SwiftPM checkouts using the established JavaScriptKit/SwiftHTML lookup pattern; remove the vendored `Packages/swift-actor-system` tree so the standalone repository is the sole source owner; and update focused locator/materializer tests and links without adding a second resolver abstraction `depends:REL-WEB.1` `parallel:none`
  - [ ] REL-WEB.3 Promote the current Unreleased changes to 0.12.0 in README, changelog, CLI/template minimum version, examples, Actor/adapter documentation, and verification boundaries, explicitly distinguishing the real Embedded generated-Actor runtime gate from the native-browser Service test and from unverified cloud deployment `depends:REL-WEB.2` `parallel:none`
  - [ ] REL-WEB.4 With the root manifest free of local-path and revision dependencies, run bounded focused Native package-generation/Actor/TLS/WSS tests, standard-WASM and Embedded-WASM generated-package compile/link gates, the real browser HTTP Service Actor test and TLS WebSocket handshake path, plus the pinned full host suite once; verify no release-scope incomplete marker, dependency override, generated artifact, or unrelated edit is included, obtain this designer’s conformance review, then commit the coherent 0.12.0 preparation on `main` after confirming branch and message and push normally to its configured upstream `depends:REL-WEB.3` `parallel:none`

- [ ] REL-INTEGRATION Verify and publish the complete release chain: from clean temporary consumers resolve the released dependency tags and a local annotated SwiftWeb 0.12.0 candidate tag without path/revision overrides, build the public products with the pinned Swift 6.4 toolchain, rerun only any gate invalidated by the candidate-tag graph, then create/push annotated `0.12.0`, publish the non-draft/non-prerelease SwiftWeb GitHub Release, and prove every released repository’s peeled tag and release target equals its `origin/main`; completion establishes the tested host, standard-WASM, Embedded-WASM, native browser, TLS, and WSS paths only, not general cloud E2E or an unmeasured performance claim `depends:REL-AS,REL-HTML,REL-SSL,REL-TLS,REL-TLSNIO,REL-WEB` `parallel:none`
