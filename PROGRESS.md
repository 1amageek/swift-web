# Progress

- [x] REL-AS Release `swift-actor-system` 0.1.0 as the single long-term owner of the transport-neutral Actor runtime while preserving the existing public/runtime semantics; completed in commit `f3f80a00e2e4c02f873e1468322c39b0f05bb75e`; API redesign, an Embedded HTTP host, cloud deployment, and unrelated hardening are non-goals `depends:none` `parallel:none`

- [x] REL-HTML Release SwiftHTML 0.16.0 for the state-invalidation API required by SwiftWeb, without changing component rendering semantics; completed in commit `1c6c7da19386de035a844ebf289e51a52bcb0c8f`; public Release https://github.com/1amageek/swift-html/releases/tag/0.16.0; pinned Native focused/full tests passed and the fresh exact-version consumer resolved, built, and ran against the same commit/tree; downstream SwiftWeb browser and generated Embedded-WASM proof remains owned by REL-WEB/REL-INTEGRATION `depends:none` `parallel:none`

- [x] REL-SSL Release swift-ssl 0.4.0 as the stable owner of the direct TLS-record destination, resumption, and TLS 1.3 compatibility changes consumed by swift-tls; completed in commit `045d6c1ccffd5adc7f2da2fd748de86f05bbde9b`; public Release https://github.com/1amageek/swift-ssl/releases/tag/0.4.0; remote peeled tag, origin/main, and fresh exact-version consumer resolution/build/run agree; broader cryptography review is a non-goal `depends:none` `parallel:none`

- [x] REL-TLS Release swift-tls 2.1.0 on the stable swift-ssl 0.4.0 contract while preserving the owned/scoped byte boundary and typed failure model; completed in commit `cbb2f5b3e3355c51cfea2edc6a8282fed16e790d`; public Release https://github.com/1amageek/swift-tls/releases/tag/2.1.0; remote peeled tag, origin/main, and fresh exact-version consumer resolution/build/run agree; native canonical plus standard/Embedded facade and exact-version Embedded dependency-consumer gates passed; broader cryptography review and benchmarking are non-goals `depends:REL-SSL` `parallel:none`

- [x] REL-TLSNIO Release swift-tls-nio 0.1.0 as the NIO adapter over swift-tls 2.1.0, preserving ByteBuffer ownership, active-handshake write buffering, backpressure/order, failure, and shutdown contracts; completed in commit `6b62dda308676c82fca08acc12eda6d747e34cbb`; public Release https://github.com/1amageek/swift-tls-nio/releases/tag/0.1.0; remote peeled tag, origin/main, and fresh exact-version consumer resolution/build/run agree; final resolved swift-nio 2.102.0 incremental build and 21 behavioral tests/3 suites (including real TCP loopback) passed; broader complete-stack security review and WASM/Embedded NIO runtime are non-goals `depends:REL-TLS` `parallel:none`

- [x] REL-WEB Prepare SwiftWeb 0.12.0 to consume only released package versions and the standalone Actor source owner while preserving the existing Distributed Actor, `.actor(_:identity:)`, `ActorGroup`, `@RemoteActor`, HTTP/WSS, and generated-package behavior; completed in commit `3b8903e`; focused Native, full host, native Service/TLS/WSS, standard-WASM browser, and generated Embedded-WASM release compile/link gates passed; no Embedded browser/cloud E2E or page-access stress benchmark is claimed `depends:REL-AS,REL-HTML,REL-TLSNIO` `parallel:none`

- [x] REL-INTEGRATION Published the complete release chain and SwiftWeb 0.12.0 at commit `db08b581827a0fcf705c17d33b1050626dbdc2c9`; public Release https://github.com/1amageek/swift-web/releases/tag/0.12.0; annotated tag, peeled commit, and remote main agree; the pinned macOS 26.2 exact-version consumer built, passed HTTP/WSS 23 tests plus Service Actor Chromium 1 test, and ran its public API probe; subsequent GitHub URL resolution preserved all 34 dependency revisions and the candidate tree; recorded locally after publication without advancing the released main commit; Embedded browser/cloud execution and unmeasured performance remain outside the proof boundary `depends:REL-AS,REL-HTML,REL-SSL,REL-TLS,REL-TLSNIO,REL-WEB` `parallel:none`

- [x] FIX-SCOPE Confirmed the implementation boundary: preserve the Standard-WASM browser runtime, require real Chromium and WebKit evidence, execute the existing Cloudflare `page-worker` Embedded host through local workerd, and exclude live Cloudflare deployment, paid resources, tags, and releases `depends:none` `parallel:none`

- [x] FIX-HTML Remove the pinned Swift 6.4 Embedded Debug SIL verifier failure from SwiftHTML `StateStore<Optional<String>>` without changing Optional or invalidation semantics; completed in commit `573aba6454604780c07ad8a7aabd0e153423fe4b`; preserved the common `Mutex` owner and exact toolchain/SDK contract with Native, standard WASM, and Embedded WASM evidence `depends:FIX-SCOPE` `parallel:none`

- [x] FIX-CLOCK Provide real monotonic deadline behavior to the Cloudflare `page-worker` Embedded deployment through one SwiftWeb Actors platform-clock injection seam and a JavaScript-hosted `ActorClock`, while keeping ActorSystemCore platform-neutral and preserving custom clocks, cancellation, shutdown, and shared-state contracts; completed in SwiftWeb commit `10ab5465af363907950c78c3e018c0713d87b8be` and swift-web-cloudflare commit `137487792ebaa0b19dc5476a2c21f8af4dc12a8f`; Native/Embedded Actors, Cloudflare ActorClock/Core local-workerd runtime, and host-consumer Embedded compile/link gates passed; proof is bounded to ActorClock/Core and workerd plus host-consumer link, not the full Cloudflare Host runtime; no live deployment, tag, or release `depends:FIX-HTML` `parallel:none`

- [x] FIX-EFFECTS Accept authored portable actor methods using typed throws while preserving authored Native/local typed signatures, generated untyped remote wrappers, wire/schema identity, and distinct application, system, and cancellation failures; completed in swift-actor-system commits `308c56105d3203045b8633e77d983153eb3bd72c` (generator/distributed implementation) and `f6af939ee1e4118745b0db4ac091a7f4f126743e` (Native/Standard-WASM/Embedded-WASM fixture); full ActorSystem validation passed with 128 tests/11 suites, and pinned Native 4.62s, Embedded 8.94s, and Standard-WASM 5.18s build/runtime gates passed against exact public dependency revisions without local edits; no new public error wrapper, Core algorithm, tag, release, or deployment `depends:FIX-CLOCK` `parallel:none`

- [x] FIX-TLS-CLOSE Remove the normal HTTPS/WSS shutdown timeout at the TLSNIO lifecycle owner and adopt that fix in SwiftWeb while preserving authenticated `close_notify`, typed stalled/unclean-peer failures, pre-full-close application-data ordering and backpressure, and the public host/TLS contracts; completed in SwiftWeb commit `36c7aae9a7a7bb581a4143649205d8d2eaf4d52d` with TLSNIO commit `d6edc14d363e5e64e4612399540b6ac63c986ff5`; exact public Git revisions and no local/edit overrides preserved for 34 dependencies; TLSNIO owner suite 24 tests/3 suites passed, and SwiftWeb HTTP host suite 23 tests/1 suite passed with the strict HTTPS/WSS test in 0.468s, shutdown before the configured 5s deadline, and no TLS failure; task-designer provenance review passed, with broader full-stack integration remaining under `FIX-INTEGRATION` `depends:FIX-HTML` `parallel:none`

- [x] FIX-EMBEDDED-HOST Execute the production generated Cloudflare `page-worker` through local workerd while preserving the Standard-WASM browser contract; completed and normally pushed in swift-web-cloudflare fixture commit `c7cb3b1216dcc75a86259c0a8173907f43186d5a`, SwiftWeb generator commit `3408c74f35143539fa4b79c0785dad72dbb795a1`, SwiftWeb process-guard commit `3dbb0850b8010dce9b86b744790e858b96338281`, and Cloudflare adoption commit `6f9d70353e65d01e1cad2dcc8a8e6b783d243844`; task-designer review and remote/tree provenance passed, and exact public SwiftWeb/Cloudflare/ActorSystem revisions with no local/edit overrides passed the pinned Debug Embedded build/link and real workerd Durable Object RPC-to-Native gate: page HTTP 200, generated Actor success, typed application failure, in-flight cancellation, actual 750ms Cloudflare-clock deadline, application-owned Core shutdown and exact post-shutdown rejection, zero active clock waits, workerd disposal, and bounded Native process cleanup; owner tests passed, with no Release/Osize, deployment, tag, or release claim `depends:FIX-HTML,FIX-CLOCK,FIX-EFFECTS,FIX-TLS-CLOSE` `parallel:none`

- [x] FIX-WEBKIT Require both counter npm commands to run the unchanged full Chromium suite and real WebKit Standard-WASM hydration, navigation-free Actor mutation, reload persistence, and eager-plus-idle readiness before reload/close; completed and normally pushed in SwiftWeb source commit `5bbf741ed66694a00722aa8000e093e38dfc5cb1`, with task-designer GO and exact remote-main provenance; the Dev Reconciler now defers materializing preparation while an active transition owns generated build inputs, preserving existing actor/Mutex state and crash/failure semantics; focused ordering red followed by 8/8 Reconciler tests, 2/2 fail-fast probes, and the pinned 2026-08-14 Standard-WASM required full Chromium-plus-WebKit gate passed with Actor 0→1 retained after reload, zero unexpected browser/HTTP/console/server diagnostics, ready eager/idle bundles, clean server exit, and no residual processes/listener; no Embedded-browser, deployment, tag, or release claim `depends:FIX-EMBEDDED-HOST` `parallel:none`

- [x] FIX-JSKIT-EXECUTOR JavaScriptKit `166dc39b6e282a0f039762381332ba6333ec809c` was reviewed, committed, and normally pushed with exact remote-main provenance; fixed Swift 6.4 Embedded WASI installs the existing factory for MainActor/default tasks plus compatible legacy entries, without Embedded SchedulingExecutor conformance or storage/Sendable changes. Existing Embedded browser example and 12 Standard owner tests passed; SwiftWeb consumes that exact public revision with no edit override, 27 Native ClientRuntime tests and both pinned Debug Standard/Embedded Chromium ABI gates passed all four 0/1 MiB fetch/host cases, exact failures/cancellation/shutdown, one copy per direction, empty diagnostics, and cleanup. SwiftWeb adoption remains part of the coherent FIX-PERF.3 commit; no tag, release, or deployment `depends:FIX-WEBKIT` `parallel:none`

- [x] FIX-PERF SwiftWeb commit `b67a766abe54229de27c010df8778328eb78b3c1` reduced only the measured browser Actor HTTP copies and adopted public JavaScriptKit `166dc39b6e282a0f039762381332ba6333ec809c` with task-designer GO; the common owner borrows the encoded frame, reuses one JavaScript typed array, and initializes the final Swift response array directly, preserving Mutex/MainActor/Sendable and exact failures. Native 37-test owner/range/HTTP/WSS evidence and default HelloWorld performance/stress evidence remain valid; pinned public Standard/Embedded Chromium probes passed all four 0/1 MiB cases with one ABI copy per direction, equal bytes, cancellation/shutdown, empty diagnostics, and cleanup. Source-backed intermediate storage accounting remains distinct from measured ABI calls and required HTTP/TLS copies; the obsolete page-runner CLI argument was removed without a new option or latency policy. No repository-wide zero-copy, full Embedded browser/cloud E2E, tag, release, or deployment claim `depends:FIX-TLS-CLOSE,FIX-EMBEDDED-HOST,FIX-WEBKIT,FIX-JSKIT-EXECUTOR` `parallel:none`

- [ ] FIX-INTEGRATION Verify the complete fixed graph at the reviewed upstream repository commits using one heavy build at a time with `--jobs 2`; prove affected Native suites, Standard-WASM Chromium and required WebKit, retain the reviewed `c7cb3b1216dcc75a86259c0a8173907f43186d5a` local-workerd Embedded host evidence because its `SwiftWebRequestReplyActorTransport` and Cloudflare driver path are unchanged by the browser-only performance and JavaScriptKit executor edits, and prove scoped commit consumption, shared-state parity, bounded performance, and process cleanup without live deploy, tag, or release `depends:FIX-HTML,FIX-CLOCK,FIX-EFFECTS,FIX-TLS-CLOSE,FIX-EMBEDDED-HOST,FIX-WEBKIT,FIX-JSKIT-EXECUTOR,FIX-PERF` `parallel:none`
  - [x] FIX-INTEGRATION.1 Record exact upstream repository commits, dependency graphs, pinned toolchain/SDK tuple, incomplete markers, and Native/Standard/Embedded storage-isolation matrix; resolve the final SwiftWeb commit and its exact JavaScriptKit revision from a new temporary consumer root and scratch state with no preexisting resolution state while allowing repository-download cache reuse, require public Git URLs with exact immutable revisions and no branch, local path, or edit overrides for that proof, retain the independently reviewed exact Cloudflare `c7cb3b1216dcc75a86259c0a8173907f43186d5a` graph for its unchanged host path, and record that a future semver release must replace unreleased revisions with released dependency versions `depends:FIX-HTML,FIX-CLOCK,FIX-EFFECTS,FIX-TLS-CLOSE,FIX-EMBEDDED-HOST,FIX-WEBKIT,FIX-JSKIT-EXECUTOR,FIX-PERF` `parallel:none`
  - [ ] FIX-INTEGRATION.2 Run the required Chromium-plus-WebKit Counter gate against the clean final SwiftWeb commit because it exercises the changed Standard-WASM browser transport; preserve the successful Actor mutation/reload behavior already observed, and correct the concrete WebKit reload failure at the document-owned HMR connection lifetime rather than filtering its access-control diagnostic; retain rather than rerun unchanged Native, page-access performance/stress, and Cloudflare workerd gates; preserve exact logs, browser/workerd reports, performance measurements, and no-leaked-process evidence `depends:FIX-INTEGRATION.1` `parallel:none`
    - [x] FIX-INTEGRATION.2.1 Retain SwiftWeb commit `2e0b967` document-close ownership, but correct the proven pre-`pagehide` WebKit retry at the HMR connection owner: select native `EventSource` whenever available, leave `CONNECTING` cursor-preserving reconnection to the user agent with no manual timer or source recreation, and on unexpected live `CLOSED` perform at most one abortable same-endpoint status probe whose body is aborted after headers, preserving 401/403 reload while exposing every other terminal status; accept query `lastEventID` first and otherwise standard `Last-Event-ID` at both the streaming host and finite compatibility route; retain event ordering, live diagnostics, script replacement, non-persisted `pagehide`, and capability-only fetch-SSE/reload-poll fallbacks without changing Actor or WASM-runtime ownership; require focused server cursor and actually served browser lifecycle red/green evidence, including cancellation of the CLOSED probe and fallback paths, before commit; designer GO, Native 47 tests and served lifecycle 12 cases passed, actual Counter native-source selection and three reload-persistence cycles had empty page/console errors with five trace-confirmed expected UA navigation cancellations (strict diagnostic exit1 retained), server exit0 and no residual listener; source commit pending parent `depends:FIX-INTEGRATION.1` `parallel:none`
    - [ ] FIX-INTEGRATION.2.2 Run the required Chromium-plus-WebKit Counter gate once on the corrected clean snapshot using the existing reusable root rather than a new cold build; when and only when `SWIFTWEB_E2E_REUSE_TEMP_ROOT` is explicit, preserve the app root's `.build`, `.swiftweb`, and existing `Package.resolved` as cache/resolution state while deleting every other prior fixture input and recreating the authored sources and manifest from the canonical Counter example, and leave the ordinary fresh-root behavior unchanged; do not skip source recompilation, initial-build edit convergence, HMR, crash recovery, or either browser assertion; require the full existing Chromium assertions plus WebKit Actor mutation and reload persistence, zero unexpected browser access-control/request/console/server diagnostics, server exit zero, and no residual process or listener; preserve the earlier failing log as the red runtime evidence and do not replace this gate with source/string assertions `depends:FIX-INTEGRATION.2.1` `parallel:none`
  - [ ] FIX-INTEGRATION.3 Complete final task-designer conformance review, update the task-control record, and normally push any approved final documentation or progress commit without creating a tag, release, deployment, or paid resource `depends:FIX-INTEGRATION.2` `parallel:none`
